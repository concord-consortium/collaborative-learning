backend:
  name: github
  repo: concord-consortium/collaborative-learning
  branch: 184540073-add-decap # Branch to update (optional; defaults to master)
  base_url: https://us-central1-cms-github-auth.cloudfunctions.net
  auth_endpoint: /oauth/auth
# local_backend: true
media_folder: "images/uploads"
collections:
  # - name: "blog" # Used in routes, e.g., /admin/collections/blog
  #   label: "Blog" # Used in the UI
  #   folder: "_posts/blog" # The path to the folder where the documents are stored
  #   create: true # Allow users to create new documents in this collection
  #   slug: "{{year}}-{{month}}-{{day}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
  #   fields: # The fields for each document, usually in front matter
  #     - {label: "Layout", name: "layout", widget: "hidden", default: "blog"}
  #     - {label: "Title", name: "title", widget: "string"}
  #     - {label: "Publish Date", name: "date", widget: "datetime"}
  #     - {label: "Featured Image", name: "thumbnail", widget: "image"}
  #     - {label: "Rating (scale of 1-5)", name: "rating", widget: "number"}
  #     - {label: "Body", name: "body", widget: "markdown"}
  # - name: "unit"
  #   label: "Unit"
  #   format: "json"
  #   folder: "src/public/units" # Should be changed to curriculum when things are set up properly
  #   path: "{{directory}}/{{slug}}"
  #   create: true
  #   fields:
  #     - {label: "Code", name: "code", widget: "string"}
  #     - {label: "Title", name: "title", widget: "string"}
  #     - {label: "Abbreviated Title", name: "abbrevTitle", widget: "string", required: false}
  #     - {label: "Subtitle", name: "subtitle", widget: "string", required: false}
  #     - {label: "Folder", name: "directory", widget: "string"}
  #     - label: "Sections"
  #       name: "sections"
  #       widget: "relation"
  #       collection: "section"
  #       value_field: "{{dirname}}/{{filename}}.{{extension}}"
  #       search_fields: ["title"]
  #       display_fields: ["title"]
  #       multiple: true
  #       required: false
  # - name: "section"
  #   label: "Section"
  #   format: "json"
  #   folder: "src/public/units" # Should be changed to curriculum when things are set up properly
  #   path: "{{unit_directory}}/sections/{{slug}}"
  #   create: true
  #   fields:
  #     - {label: "Title", name: "title", widget: "string"}
  #     - {label: "Type", name: "type", widget: "string"}
  #     - label: "Unit"
  #       name: "unit_directory"
  #       widget: "relation"
  #       collection: "unit"
  #       value_field: "{{directory}}"
  #       search_fields: ["title", "code"]
  #       display_fields: ["title"]
  #     - label: "Content"
  #       name: "content"
  #       widget: "code"
  #       default_language: "JSON"
  #       output_code_only: true
  #   view_filters:
  #     - label: Bio4community
  #       field: unit_directory
  #       pattern: bio4community
  #     - label: Comparing and Scaling
  #       field: unit_directory
  #       pattern: comparing-and-scaling
  #   view_groups:
  #     - label: Unit
  #       field: unit_directory
  - name: sections
    label: Curriculum Sections
    label_singular: 'Curriculum Section'
    format: json
    folder: src/public/units
    # folder: units
    # create: true
    # adding a nested object will show the collection folder structure
    nested:
      depth: 6 # max depth to show in the collection tree
      summary: '{{title}}' # optional summary for a tree node, defaults to the inferred title field
    fields:
      - label: Title
        name: title
        widget: string
      - label: Content
        name: content
        widget: json
    # adding a meta object with a path property allows editing the path of entries
    # moving an existing entry will move the entire sub tree of the entry to the new location
    # meta: { path: { widget: string, label: 'Path', index_file: 'section' } }
