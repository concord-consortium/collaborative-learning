import { observer } from "mobx-react";
import React from "react";

export const SortWorkHeader:React.FC = observer(function SortWorkView(){
  return (
    <div key="sort-work-header" className="sort-work-header">
      dropdowns
    </div>
  );
});
